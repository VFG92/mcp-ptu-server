{
  "name": "@mcp-ptu-server/ui",
  "version": "1.0.0",
  "description": "UI visualization components for MCP PTU Server",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outfile=dist/workflow-visualizer.js --format=iife --global-name=MCPWorkflowUI --minify",
    "dev": "esbuild src/index.tsx --bundle --outfile=dist/workflow-visualizer.js --format=iife --global-name=MCPWorkflowUI --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}

